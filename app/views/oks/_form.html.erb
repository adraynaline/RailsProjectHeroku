<%= form_for(@ok) do |f| %>
  <% if @ok.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ok.errors.count, "error") %> prohibited this ok from being saved:</h2>

      <ul>
      <% @ok.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
